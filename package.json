{
  "name": "junrys-social-med-api",
  "version": "1.0.0",
  "description": "Simple API for downloading social media videos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "mkdir -p bin && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o bin/yt-dlp && chmod +x bin/yt-dlp || echo 'yt-dlp install failed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "engines": {
    "node": ">=14"
  }
}